update
install git
mkdir test
cd test
git init
git remote add origin ssh key copy
nano try1.txt //this is first commit.
git status
git add *
git commit -m "this first commit"
git config --global --edit
git commit -m "this is first commit"
git push origin master
ssh-keygen enter enter enter
cat /home/shuhari/.ssh/id_rsa.pub
setting ssh-keygen ...........new key
test copy this ssh key
git push origin master
git commit -m "this ia my pc"
git pull origin master
git pull origin master --allow-unrelated-histories
git commit -m "this is my pc"
git pull origin master
ls
git commit -m "this is my"
git push origin master
go to repo check 2 files are created




